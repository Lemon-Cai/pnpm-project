
const LowCode = () => {

  // const { status, data} = useLogin()
  
  return (
    <div>
      {/* 左 */}
      <div></div>
      {/* 中 */}
      <div>
        {/* {
          data?.map(item => (
            <div key={item.id}>{item.full_name}</div>
          ))
        } */}
      </div>
      {/* 右 */}
      <div></div>
    </div>
  )
}

export default LowCode